import styled from "styled-components";

export const Container = styled.main`
    width: min(130rem, 90%);
    margin: 0 auto;
    padding: 8rem 0;
    display: flex;
    flex-flow: row wrap;
    align-items: center;
    justify-content: center;
    gap: 5rem;
`;
